{
  "name": "incidents-mcp",
  "version": "1.0.0",
  "description": "MCP server for incident details (stdio or Streamable HTTP)",
  "main": "incidents.js",
  "bin": {
    "incidents-mcp": "incidents.js",
    "incidents-mcp-http": "incidents-http.js"
  },
  "scripts": {
    "start": "node incidents.js",
    "start:http": "node incidents-http.js"
  },
  "keywords": ["mcp", "incidents"],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "express": "^5.2.1"
  }
}
